const pool = require("../db");

const getRecord = async () => {};

const updateRecord = async (data) => {
  console.log(data)
  // 요청 받아서 DB에 저장
};

module.exports = {
  getRecord,
  updateRecord,
};
